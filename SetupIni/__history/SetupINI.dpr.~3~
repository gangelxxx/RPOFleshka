program SetupINI;

uses
  Forms,
  UnitMain in 'UnitMain.pas' {Form1},
  UnitDir in 'UnitDir.pas' {FormDir};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TForm1, Form1);
  Application.CreateForm(TFormDir, FormDir);
  Application.Run;
end.
